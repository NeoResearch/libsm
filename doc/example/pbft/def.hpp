#pragma once
#include <iostream>
#include <fstream>
#include <vector>
#include "../../../src/std.hpp"
struct GLOBAL
{
    static const int F = 1;
    static const int N = 4;
    static std::ofstream file[N];
};

// ASSUME: signature assumuption
